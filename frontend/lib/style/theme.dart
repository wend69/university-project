import 'package:flutter/material.dart';



class MainTheme {
  static const Color primaryColor = Color(0xFF00FCAC);
  static const Color boxColor = Color(0xFF052017);
  static const Color boxBorderColor = Color(0xFF027049);
static ThemeData appTheme = ThemeData(
    primaryColor: primaryColor,
    scaffoldBackgroundColor: Colors.black,
  );
}
